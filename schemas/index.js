const userSchema = require('./userSchema');
const loginSchema = require('./loginSchema');
const categoriesSchema = require('./categoriesSchema');
const postSchema = require('./blogPostSchema');

module.exports = {
  userSchema,
  loginSchema,  
  categoriesSchema,
  postSchema,
};